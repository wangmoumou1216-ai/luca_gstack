interface:
  display_name: "Implement"
  short_description: "Compile approved task cards into controlled execution"
  default_prompt: "Use $implement to compile this gated task plan into approved U-IDs and execute them through Orchestrator."
policy:
  allow_implicit_invocation: true
