interface:
  display_name: "To Tickets"
  short_description: "Publish a gated task plan as tracer-bullet tickets"
  default_prompt: "Use $to-tickets to publish this exact gated task plan as tracer-bullet tickets after I approve the preview."
policy:
  allow_implicit_invocation: false
