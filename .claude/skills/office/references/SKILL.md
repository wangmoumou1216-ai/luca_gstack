---
name: references
preamble-tier: 0
description: |
  只读参考资产目录，供其他 skill 读取。不可作为独立 skill 调用。
---

# references — 共享参考资产

此目录包含只读参考文件，供各 skill 的 SKILL.md 中 `参考 references/` 指令使用。

**不可直接调用。** 没有可执行的 Phase 或流程。

## 内容列表

- `handoff-protocol.md` — Handoff summary 格式规范（Handoff Summary Protocol 标准）
- `ai-native-design-framework.md` — AI Native 设计框架参考
- 其他参考文档（按需新增）
