interface:
  display_name: "Resolving Merge Conflicts"
  short_description: "Recover both intents before resolving a real conflict"
  default_prompt: "Use $resolving-merge-conflicts to inspect this real conflict and propose a resolution without staging or advancing."
policy:
  allow_implicit_invocation: true
