version: 1
rules:
- id: R-20260522-001
  status: active
  severity: medium
  type: quality_rule
  scope:
    skills: [office]
    scenes: ["*"]
  rule: "office: CLAUDE.md 路由表与 SKILL.md Step 3 列表必须保持一致，任一变更须同步另一处"
  source_observations:
    - O-20260522-001
- id: R-20260523-001
  status: active
  severity: medium
  type: quality_rule
  scope:
    skills: [office, idea, ux-audit, brainstorm]
    scenes: ["*"]
  rule: "office/route: 项目上下文门禁优先于场景B功能优化路由；“老项目/已有项目/继续项目”先确认或切换项目，再决定 skill。"
  source_observations:
    - O-20260523-001
- id: R-20260525-001
  status: active
  severity: medium
  type: quality_rule
  scope:
    skills: [html-prototype]
    scenes: ["*"]
  rule: "html-prototype: 母版选择必须作为显式询问项；未获确认前不得把 framework/list-page.html 写成强制约束。"
  source_observations:
    - O-20260525-001
- id: R-20260607-001
  status: retired  # 2026-07-21 裁决：外部知识件三年休眠（不路由/无门禁/实证零使用），advisory 注入=假信号，停用规则、保留磁盘安装
  severity: medium
  type: quality_rule
  scope:
    skills: [ux-brainstorm, design-brief, open-design, html-prototype, ux-audit, figma-demo]
    scenes: ["*"]
  rule: "外部知识库 ui-ux-pro-max 在精确节点给决策垫底(只读)：ux-brainstorm Phase4 方案探索→每个方案 query #1 取 UX/图表/字体选型依据；design-brief Phase3 状态覆盖+Phase6 组件映射→query table/form/empty-state 规则与 --stack shadcn 映射；ux-audit→取规范当评审标尺。命令 python3 ~/.agents/skills/ui-ux-pro-max/scripts/search.py <q> --domain ux|typography|chart [--stack shadcn] -n N。品牌锁：其配色/风格输出为通用非绑定值必须丢弃，FxUI #FF8000+文字色+framework/母版唯一权威(SF-001/SF-002)；禁 ui-ux-pro-max 的 --persist/--design-system 标志（指 search.py 的标志，与 open-design 的 designSystemId 选择无关）。"
  source_observations:
    - O-20260607-extskill
- id: R-20260607-002
  status: retired  # 2026-07-21 裁决：外部知识件三年休眠（不路由/无门禁/实证零使用），advisory 注入=假信号，停用规则、保留磁盘安装
  severity: medium
  type: quality_rule
  scope:
    skills: [design-brief, tech-spec]
    scenes: ["*"]
  rule: "需要 token 体系工程化时可参考外部 subagent design-system-architect 及 skills(visual-design-foundations / design-system-patterns)。但 brand-tokens.md #FF8000 是固定输入不是可设计项；禁多品牌/暗色/白标；framework/ 只读；它们从属于 open-design + brand-tokens 权威。"
  source_observations:
    - O-20260607-extskill
- id: R-20260607-003
  status: retired  # 2026-07-21 裁决：外部知识件三年休眠（不路由/无门禁/实证零使用），advisory 注入=假信号，停用规则、保留磁盘安装
  severity: high
  type: quality_rule
  scope:
    skills: [ux-research, ux-audit]
    scenes: ["*"]
  rule: "竞品分析可用外部 skill extract-design-system 抽竞品站 token 作研究输入(npx extract-design-system <url>，需 chromium)。输出仅参考必须回源对账 brand-tokens.md，#FF8000 与 ≤3 用色规则优先；禁止对本仓库/framework/ 跑 audit(它会建议覆盖品牌色)；仅在激活项目目录内运行，输出已 gitignore 勿入 env repo。"
  source_observations:
    - O-20260607-extskill
- id: R-20260616-001
  status: active
  severity: high
  type: quality_rule
  scope:
    skills: [open-design]
    scenes: ["*"]
  rule: "open-design 默认走桌面端生成(stage 项目+brief.md+DS → 用户在 OD 桌面端按生成 → '拉回来' recover)，走订阅会话可靠。headless(/api/chat) 为 opt-in：仅用户显式要求才跑；本 session 实测不稳(生成慢 >2.5-3min + daemon SIGTERM 重启)，失败重试上限 1 后降级 OD 桌面端生成。daemon UP 但 headless 坏时降级目标=同栈桌面端，非 magicpath/html-prototype(后者仅 daemon 真不可达)。鉴权旁注：OD spawn env 含 USER→走订阅正常，缺 USER 才回退 API-credit(OD 已满足，非故障原因)。详见候选 SC-20260616-001。"
  source_observations:
    - O-20260616-od-headless-credit
