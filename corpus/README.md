# corpus/（此目录当前未被任何 skill 实际使用——2026-07-02 对齐说明）

**这个 fork 根目录的 `corpus/` 不是 L0 语料的实际来源。** 早期脚手架设想在这里放会议纪要/历史PRD/UX页面，或软链 Obsidian vault。但实际落地的 `muse-req-triage`（见其 SKILL.md Phase 0）设计是：语料由用户在触发 Loop 时直接提供（贴文本、指定文件路径、或像 Phase 0 手填测试时那样用 `lark-cli` 现拉飞书妙记/Figma MCP 现拉设计稿）——不依赖一个预先填好的固定语料目录。

**如果未来确实需要一个稳定的语料落点**（比如反复处理同一批历史资料），可以在这里放文件或软链 Obsidian vault，但目前没有任何 skill 会自动读取这个目录——不要假设把文件放进来就会被 Loop 拿到，必须显式告诉触发 Loop 的那次调用语料在哪。
