# P6 contract preparation and historical reuse audit

Verdict: **FAIL for live-readiness of the initial preparation snapshot; PASS for the bounded historical reuse decisions below; UNKNOWN for final P6/release closure.** Default stance REFUTE. This is a read-only contract review, not the full final release review. Parent owns fixes; later parent edits require final independent readback.

Initial reviewed identities:

- runner: `66727557563c806cf1e5ff542a6fbab0aef8aac6b55da93d2eb6d36a24e3b652`
- fixtures: `161b03cffaea9a29e155bc5a4a6840ed5dbabb9394e1deb1b381f87a3b10e496`
- fixture tests: `d7e627e303eaf58f633ccb906c917f06f3dbae98bf7c7b49aac5ae733fa3c7b6`
- historical raw: `c5ebd6141c3c045ae4a34528d395385cd67fcbbd2efd8ea9715f1caaeb728a20`

Authority read: repository AGENTS/CONTEXT/catalog/manifest; project-session, framework-maintenance, cross-harness, long-session; routing-chain-check R4; office, code-review, code-hygiene; current page-context/open-design; complete branch plan including S9/R1–R5, mainline checkpoint, P6 preparation/reuse record, R5 closure/validation, and B4 preservation/ref envelopes. Historical complete file outputs were compared directly with current owners. No live model, network, source mutation, staging, or publication was performed.

## 1. Minimal freeze corrections

**Important FAIL: required page contract is unreachable in candidate scorer.** Initial runner lines 599–633 admits root/CONTEXT literal references, manifest targets, catalog skill authorities, then only expands selected owners already admitted. Neither root/CONTEXT/manifest/catalog contains a page-context edge. F13 requires page-context and catalog; F10-v2 requires page-context. An in-memory execution of the actual unchanged function independently showed both paths unreachable on both harnesses. Reading them fails candidate trace policy; omitting them fails requiredTargets. F14 can also be penalized for correctly following open-design's page contract.

Minimal fix: select the actual catalog-listed OPEN_DESIGN contract, then admit explicitly enumerated ordered edges OPEN_DESIGN → PAGE_OWNER and, for F13, PAGE_OWNER → PAGE_CATALOG. Require each source owner already admitted, the exact backtick target in that source, and regular in-root non-symlink target. F14 should admit its applicable page contract. Do not recursively admit every neighbor, seed arbitrary targets, or modify roots solely to satisfy a test. Test missing edges, unrelated neighbors, and out-of-scope/symlink rejection with the production matcher.

**Important contract ambiguity: C-VALID's generic receipt does not name the actual mandatory package.** Initial fixture lines 54–62 gives whole-page.png/location.txt, while current open-design Phase 1/3D requires brief.md + page-reference.json always, adding reference.png when confirmed. Page-context's generic screenshot/location contract alone permits the abstraction, but claiming it meets the current actual stage gate is ambiguous. Minimal fix: use the real three package names and synthetic bytes; retain missing, changed, wrong-target, local-only, and upload-only negatives and only C-VALID acceptance. Synthetic receipts remain synthetic and never prove external reception.

**Freeze plumbing remains required:** replace draft-only descriptions/version and test expectations together; update describe/live_ready to report actual reviewed readiness; preserve the old draft hashes/reviews. Bind release identity and exact source/fixture/schema/scorer/commands before any dispatch. Full scorer identity must continue to include runner plus imported fixture/test dependencies. Keep legacy fixtures and scores intact.

**Governed fallback binding PASS on current source inspection:** exact set is `SC-20260905-001, SF-003, SF-005, SC-20260523-001, SC-20260523-002, SC-20260523-003`. Both root blocks match every allowlisted promoted fact's exact ID/domain/text and all six facts are stable. SC-20260905-001 explicitly supersedes SF-002. R5's new SC-20260905-002/003/004 are not fallback IDs. The factory's syntax-only arbitrary-ID check is not governance validation: live freeze/pre-dispatch must compare the actual allowlist and both projections, and reject a syntactically valid but different set. Keep synthetic IDs confined to local binding tests.

## 2. Baseline fairness

The retained baseline `/private/tmp/luca-agent-context-ab.p3S7fo/baseline` lacks both page-context.md and page-library/catalog.json, while containing old DB/OD/UX skills. The approved branch plan explicitly says new-capability absence is a feature difference, not a regression.

The same F13/F14 schema/claim scorer may be run honestly as **descriptive attainment of the new target policy**, preserving all raw outcomes. It cannot establish equal-capability regression, preservation of every old behavior, or causal benefit of context slimming. Baseline and candidate also have intentionally different owner-reachability obligations; composite PASS values are not an apples-to-apples treatment estimate. Label these differences before executing and in the final report. Do not copy new owners into baseline, relax expected values, count a baseline failure as an unsafe regression merely because policy changed, or rerun for a better answer.

Current F13's semantic mapping is supported by the catalog: list/filters covers combined conditions and list/records covers comparison/selection; mobile layout is structural reference. Its pending/adoption, rejection, no-match, stale selection, and independent target/write-permission expectations agree with page-context. F14 correctly declares partial S9 coverage. P16 score arithmetic and P17/P18 actual flow paths are not tested by the sampled fields/retention labels.

## 3. Historical reuse

**Accept only bounded historical unchanged-obligation evidence:** Codex candidate F1, F2, F3, F4-direct, F4-multi, F4-stop, F5, F6, F7, F8, F11, F12 (12 rows). Preserve original protocol/hash/context/model-null metadata. These are not current-context reruns and do not fill Claude cells.

Direct complete-output comparisons found byte identity for agent-context-manifest, project-session, framework-maintenance, extraction-bar, correction-attribution, memory/README, harness-boundary, luca-app, cross-harness, and long-session. Changes to CONTEXT concern retired CRM/token/template authority only. Catalog changes concern figma-layer retirement and HTML/OD/UX/MagicPath descriptions; compare, writing-for-agents, ux-writing and route-class expectations remain unchanged. Routing-map changes retire figma-layer. Plan-agent changes tool-choice/fallback paths, leaving the five Plan/approval claims intact. The two pre-SF root copies in `/private/tmp/br-page-sf002-before-20260905/{4,5}` match B0's exact root hashes, and their diff to current roots consists solely of the SF-002 replacement line. These facts justify limited semantic reuse, not hash-identical whole-context equivalence.

**Reject final-policy reuse:** old candidate F9 and F10. F9 PASS encodes the canceled fallback/template policy; F10 FAIL remains a real historical failure. New versioned cells must establish the approved replacement policy. Preserve the earlier F4-stop failure at raw line 478 even though selected line 482 passed. Preserve baseline F5 failure at line 477. Original selected-18 counts remain 16 PASS / 2 FAIL.

The four new Opus-4.6 baseline rows remain their original v20 measurements. P6 preparation explicitly reports F2/F3 startup omissions and supplemental v21 scope UNKNOWN for F2/F3/F5; do not convert those to new full-compliance PASS. Old Codex actual_model=null and mixed Claude configurations prevent model-matched causal/reliability claims.

**Still UNKNOWN/blocking final closure:** final frozen candidate behavioral cells; real authorized OD project/body/reference-byte readback and actual page-adoption evidence if a page is attached; final full S9 preservation assurance assembled from B4's nine grouped passing criteria plus its deferred C10 and current final source identities; root-budget mutation; final safety/semantic/mutation verification; cold independent final release review; exact staged snapshot and publication gates. B1's SVG mixed-source containment limitation remains USER_WAIVED, never PASS and never a reason to reopen its test loop. F14 alone cannot discharge its documented P01–P19 gaps.

## 4. Proposed serial order for the authorized remaining 16 cells

1. baseline / Claude / F13-page-handoff
2. baseline / Codex / F13-page-handoff
3. baseline / Claude / F14-flow-preservation
4. baseline / Codex / F14-flow-preservation
5. candidate / Claude / F4-stop
6. candidate / Claude / F2
7. candidate / Claude / F3
8. candidate / Claude / F5
9. candidate / Claude / F9-v2
10. candidate / Codex / F9-v2
11. candidate / Claude / F10-v2
12. candidate / Codex / F10-v2
13. candidate / Claude / F13-page-handoff
14. candidate / Codex / F13-page-handoff
15. candidate / Claude / F14-flow-preservation
16. candidate / Codex / F14-flow-preservation

Use exact frozen root, `--arm`, `--harness`, `--fixture`, `--trials 1 --concurrency 1`, append-only `--output`, explicit batch ID, and `--claude-model claude-opus-4-6`; exact fallback binding for F9-v2. Candidate `--require-pass` makes command intent explicit, though runner already stops candidate failure. Never use `--fixture all`, resume-valid, calibration, best-of, automatic repair/repeat, or extra paid calls. Inspect the single appended row and return code before dispatching the next independent process; a shell loop without this gate can bypass runner-local stop state.

Infrastructure failure, actual-model mismatch, release/context/scorer drift, or source-scope violation stops either arm. Pure baseline behavioral FAIL stays a measurement and may continue within that baseline phase. First candidate non-PASS, including missing full reads or UNKNOWN scope, halts remaining candidate and successor dispatch; keep failed attempt and unrun cells explicit. Budget counts attempted processes, not only successes. A later fix does not grant a repeat.

## Separate archival whitespace observation

Parent's full diff-check reports one new blank line at archive/superseded-facts-2026.yaml:52. Direct byte extraction shows current lines 44–52 equal original SF-001 lines 3–11 in `/private/tmp/br-page-r5-before-jxfd26ba/memory/semantic/promoted-facts.yaml`, including final `search-only\n\n`. The blank is original record-block data, not an introduced extra separator. Retain bytes and record only this exact expected diagnostic, assert the full diagnostic set contains no others, and verify original-block equality. Do not globally disable blank-at-eof or trim preserved evidence merely to obtain green.

No final release PASS is granted by this report. Parent began editing the fixture/test files after the initial snapshot review; these fixes require a fresh bounded terminal review.
