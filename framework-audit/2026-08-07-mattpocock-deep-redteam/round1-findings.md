# Round 1 synthesis — existing capabilities and adapter foundation

> State: `ROUND_1 COMPLETE / HANDSHAKE BLOCKED / REVISION ISSUED`.  
> Sources: independent lineage/need redteam, independent harness/safety redteam, the earlier
> need-first redteam, and the root direct-read ledger. Votes were not averaged; supported blockers
> were unioned.

## 1. What Round 1 overturned

1. **The denominator was wrong.** The initial 47 rows cover behavior/content only. Ten governance,
   provenance, route, and wiring atoms can fail independently. Round 2 denominator is 57, frozen in
   `inventory-refreeze-r2.md`.
2. **Static presence was being over-read.** Of 57 atoms the lineage redteam found
   `STATIC_STANDS 48 / MODIFIED 8 / REFUTED 1`; none of those labels is a dual-harness live PASS.
3. **The old four-track consensus was too narrow.** It addressed codebase reachability, debug
   redaction, authoring doctrine, and questionnaire disposition, but omitted unsafe resolving,
   destructive rollback, project traversal, untrusted shell interpolation, orchestration collapse,
   stale governance, and false-green verification.
4. **The earlier quality-gate PASS is non-terminal.** It judged the old scope and cannot authorize
   the expanded plan.

## 2. Round 1 blockers

| ID | Finding | Evidence/attack | Forced revision |
|---|---|---|---|
| R1-B01 | Coverage and checker false-green | 47 omitted MP-048…057; registration/routing checks pass while external targets are absent | G0 uses a 57-row manifest, route/catalog/reference join, negative mutations, and separate live receipts |
| R1-B02 | Codex is routed to absent `codebase-design` and `resolving-merge-conflicts` | Cross-harness route entries exist; only Claude personal targets exist; live Codex catalog lacks both | G2 quarantines routes before building full shared, harness-adapted candidates |
| R1-B03 | Resolving safety reconciliation is false | Governance said never-abort would not be imported; live file remains byte-identical and says always resolve, never abort, stage everything, commit | G1 blocks current route; G2.3 restores pause/abort, scoped staging, and separate commit authority |
| R1-B04 | FUSION rollback can destroy later commits and dirty work | Runbook defaults to `reset --hard` and then invokes unsafe resolving | G1.4 requires exact revert/atomic rollback and destructive-gate fixtures |
| R1-B05 | Project name can escape the projects root and pin is written before command success | CLI accepts dot segments; hook claims the value pre-execution and can rewrite a later compound segment | G1.1 adds one validator and a post-success pin transaction with failure-preserves-old-pin tests |
| R1-B06 | Requirement text can execute shell | triage guidance embeds raw `statement_ears` inside a double-quoted `echo` command | G1.2 uses byte-preserving structured stdin and hostile-text side-effect fixtures |
| R1-B07 | Shared systematic-debugging leaks secrets and arbitrary pasted errors | actual env value expansion, `env | grep`, raw HITL echo, and environment logging guidance | G1.3 repairs all source/output surfaces, source watch, helper correctness, and multi-artifact canaries |

## 3. Round 1 major findings

| ID | Finding | Revision owner |
|---|---|---|
| R1-M01 | Plan/work/oracle have no truthful Codex execution layer; Plan route success proves only routing | G3 logical roles + explicit harness adapters + distinct-context receipts |
| R1-M02 | brainstorm’s `task()`/`background_output`/Oracle paths silently collapse to same-thread reflection | G3 forbids calling self-review an Oracle pass and defines fail-visible degradation |
| R1-M03 | Human gates are not behaviorally mapped for no-widget/headless modes | G3 widget/plain-text wait/`NEEDS_CONTEXT` invariant |
| R1-M04 | Handoff redaction is prose-only and its writer can truncate an existing file | G1.4 safe writer, collision test, secret scan, required-criteria assertion |
| R1-M05 | `teach` is called zero-touch but writes a multi-file workspace in the current directory | G2.4 explicit personal exception plus dedicated teaching root/no-repository-write gate |
| R1-M06 | code-recon describes machine-global unpinned install as downstream-project local | G3 explicit scope/authority, exact pin, rollback or truly project-scoped mechanism |
| R1-M07 | Codex project guard advises a `Read` tool unavailable in the observed tool surface | G1.1 actual Codex read-only path, write denial retained |
| R1-M08 | Codex verifier is non-hermetic and its assertions miss exact external targets and logical roles | G0 environment isolation, truthful BLOCKED, exact-name assertions |
| R1-M09 | model effort and hook registration prose contradict their own SSOT | G0 drift corrections and mutation checks |
| R1-M10 | codebase parallel design launches 3+ agents without the Plan gate/read-only guarantee | G2.2 Plan gate, isolated read-only proposals, explicit block if independent contexts unavailable |
| R1-M11 | systematic helper can split filenames, swallow test failure, and emit false “not found”; references missing skills | G1.3 helper semantics and negative fixtures |
| R1-M12 | registration sync’s “addressed” claim exceeds its implementation; REG3 is only a warning | G0 exact blocking assertions for external targets, nested refs, and required surfaces |
| R1-M13 | current parity file proves strings/presence, not two-harness behavior | G0/G5 separate T/E/D/V live receipts |
| R1-M14 | adoption/pin/vetting ledgers contain stale topology and status claims | G0 reconciliation; G5 governed W⑨ transaction after activation only |
| R1-M15 | usage evidence is uneven; most installed/embedded capabilities have no direct observed invocation | G0 distinguishes static/reference/live evidence; no demand claim without outcome evidence |

Minor issues retained as owned work: code-recon batches three blocking questions; TDD points to a
nonexistent `code-review` name instead of the local capability; a systematic-debugging example is
bound to a specific personal project. They cannot be silently dropped, but they do not independently
defeat the revised plan if their named track closes them.

## 4. Existing capability disposition after Round 1

- **Direct installs:** TDD content stands statically; codebase-design content stands but reachability
  and parallel dispatch are modified; resolving intent-reconstruction stands but its safety promise
  is refuted; teach content stands with a scope correction.
- **Authoring/code-hygiene ports:** content largely stands; cross-harness pointer and behavior proof
  remain incomplete.
- **Systematic-debugging port:** ranked falsifiable hypotheses, probe discipline, seam honesty, and
  HITL concept stand; secret handling and helper behavior block use as currently written.
- **Planning/brainstorm/tech-spec ports:** content decisions stand; adapter execution and human gates
  remain materially incomplete.
- **Handoff/triage/code-recon ports:** intended governance stands; writer safety, untrusted input,
  dependency reachability, and install-scope claims require repair.
- **quick-research/registration/CHANGELOG:** static wiring stands; registration’s claimed coverage
  must be narrowed until G0 expands the checker.

## 5. Upstream update decisions retained or narrowed

- TDD’s new codebase pointer remains demand-triggered DEFER. Fixing an already-broken route is not
  evidence that the TDD refresh has local value.
- The diagnosing redaction direction stands but expands to all source, script, transcript, and
  artifact surfaces; prose alone cannot pass.
- Writing-for-agents reduces to one environment/cache doctrine sentence in the local authoring SSOT.
- Logic demo and questionnaire remain one-off demand pilots before any registration.
- Grilling rounds, wizard, ask-matt phase tree, and wait-what-as-skill remain rejected under their
  explicit re-entry criteria.

## 6. Direct-read integrity statement

The root ledger fully read the exact 38 pinned origin blobs and 20 current candidate blobs, plus the
live targets, registries, adapters, and audit lineage it lists. Two batched reads were truncated;
both complete batches were invalidated and the files reread individually. This is a recorded failure
of the gate, not retroactively counted coverage.

The independent lineage reviewer fully read the 21-record adoption log, registries/pins/vetting,
inventory/mapping/verdict pack, live targets, and installed skills, with physical EOF or FILE_END
checks. It did not read every historical verdict or replay real Claude/Codex behavior. The safety
reviewer fully read the relevant contracts, skills, nested references, scripts, hooks, and verifier,
while deliberately leaving unrelated office skills and historical logs outside its declared scope.

Therefore source-boundary evidence is strong enough to revise the plan, but the ledger remains
`ROOT-RECEIPT + TWO INDEPENDENT SCOPED REPLAYS`, not a claim that all 57 units have live behavioral
proof. Round 2 must attack the 57-row refreeze and revised plan directly.

<!-- FILE_END: round1-findings.md -->
