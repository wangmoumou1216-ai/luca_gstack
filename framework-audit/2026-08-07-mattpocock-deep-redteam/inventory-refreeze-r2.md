# mattpocock-derived capability inventory — Round 2 refreeze

> Canonical denominator for Round 2: **57 atomic units**.  
> `capability-inventory.md` remains the immutable Round 1 behavioral subtotal (MP-001…047). This
> refreeze adds the ten governance/reachability atoms that the independent lineage redteam proved
> can fail separately. The two files together are the Round 2 inventory.

## 1. Denominator reconciliation

| Layer | Count | Meaning |
|---|---:|---|
| MP-001…047 | 47 | Live behavior/content atoms from direct installs and embedded ports |
| MP-048…057 | 10 | Governance, route, pin, provenance, and wiring atoms omitted by Round 1 |
| **Total** | **57** | Full audit denominator for the user’s cross-surface request |

Independent Round 1 classification before plan revision:

- behavioral subtotal: `STANDS 44 / MODIFIED 3 / REFUTED 0 / UNKNOWN 0`;
- full denominator: `STATIC_STANDS 48 / MODIFIED 8 / REFUTED 1 / UNKNOWN 0`.

`STATIC_STANDS` means the behavior or record is present under read-only review. It is not a
`DUAL_HARNESS_LIVE_PASS` and must never be presented as one.

## 2. Added atoms

| ID | Independently failing atom | Current Round 1 state |
|---|---|---|
| MP-048 | `codebase-design` route → Claude/Codex catalog/target/nested-reference reachability | **MODIFIED/BLOCK**: route is cross-harness; target is Claude-only |
| MP-049 | `resolving-merge-conflicts` route → Claude/Codex catalog/target reachability | **MODIFIED/BLOCK**: Codex target absent |
| MP-050 | resolving safety reconciliation promised by governance vs live `never --abort` behavior | **REFUTED/BLOCK**: live file is byte-identical to unsafe upstream wording |
| MP-051 | TDD refresh file footprint and installed-pin completeness | STATIC_STANDS; old five-support-file prose is stale and must be superseded |
| MP-052 | `teach` personal/no-route/single-harness exception and write-scope truth | STATIC_STANDS with scope MAJOR; “zero touch” is inaccurate |
| MP-053 | skill-authoring doctrine wiring into CLAUDE, AGENTS, runbook, and parity checks | **MODIFIED**: AGENTS pointer and behavior parity are incomplete |
| MP-054 | systematic-debugging matt port provenance and upstream watch | **MODIFIED**: local creation lineage exists; matt source watch is absent |
| MP-055 | registration checker’s actual S19/package integration and blocking semantics | **MODIFIED**: REG3 warns; external targets/nested refs are not asserted |
| MP-056 | plan-agent PROGRESS appendix wiring | STATIC_STANDS |
| MP-057 | plan-agent CLAUDE/Codex parity wiring | **MODIFIED/BLOCK**: no Codex plan role; hardcoded Claude model aliases remain |

## 3. De-duplication rules

- `writing-great-skills` contributes seven unique content mechanisms, not “six plus duplicated
  metavocabulary”; its separate cross-harness pointer is MP-053.
- domain-modeling’s Oracle terminology is counted once in brainstorm; its CLAUDE persistence rule
  and extraction trigger remain separate consumption behaviors.
- 2026-07-23 D2/D3/D5 count only their later deltas, not the full earlier capability again.
- TDD removed two independent support documents; a delegated refactoring move is not a third
  behavior benefit.
- deletion test may be counted as a definition in codebase-design and as a separately failing
  consumer action in code-recon, but not double-weighted as one adoption benefit.

## 4. Round 2 replay gate

A reviewer must replay all 57 rows across upstream lineage, live target, governance/route, and
Claude/Codex T/E/D/V applicability. Any newly found independently failing atom expands the
denominator again and invalidates the Round 2 freeze; because the loop is capped at two rounds,
that outcome yields `NO_HANDSHAKE` rather than an automatic third round.

<!-- FILE_END: inventory-refreeze-r2.md -->
