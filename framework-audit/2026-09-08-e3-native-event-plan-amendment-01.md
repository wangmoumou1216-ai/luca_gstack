# E3 native event identity — approved evidence amendment 01

Parent plan: `E3-NATIVE-EVENT-20260908-01`

Parent plan SHA-256:
`b95ad7aa07cc955e4cf6d6f99ed37e410a15827ed31e6783f0386fdf810224b1`

Authority: the user replied `确认` after the assistant presented the three explicit continuation
conditions. This record narrows that confirmation to the effects and evidence substitutions listed
below; it does not infer that external state has already changed.

## Approved

1. One minimal isolated Codex probe may use invocation-scoped
   `--dangerously-bypass-hook-trust` to run the already-inspected temporary collector. The bypass
   must not persist trust, modify global/repository configuration, or grant broader filesystem
   authority.
2. The safe-distance-2 positive case may use the preserved historical real native record as an
   immutable regression fixture, paired with current-version distance-1 evidence and an
   intervening-user fail-closed negative fixture.
3. Same-boundary identical-text events may use a strict synthetic native-schema fixture, paired
   with the current-version real evidence that multiple distinct native events share one boundary.
   Text equality remains neither identity nor replay evidence.

## Not inferred or approved

- Claude Code access is not presumed restored; a fresh probe must still succeed.
- The concurrent Memory governance session is not presumed complete and retains ownership of the
  shared hook/test files until the user explicitly reports release.
- No persistent hook trust, restart, activation, `--no-verify`, E3 implementation commit, push,
  v26 A/B call, Memory mutation, or downstream-project access is authorized.
- This amendment does not waive explicit Codex PreToolUse/Stop evidence or the required Claude
  native evidence.

## Execution rule

The corrected Codex invocation must preserve the earlier argument-parse failure as evidence and run
only once. A new failure stops the probe; it may not be replaced by fallback or best-of retries.

<!-- FILE_END: E3-NATIVE-EVENT-PLAN-AMENDMENT-01 -->
