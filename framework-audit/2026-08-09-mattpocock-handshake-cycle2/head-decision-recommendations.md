# HEAD candidate 独立裁决建议

> 状态：`DONE_WITH_CONCERNS`（裁决完成；原 73 行中 1 行被独立 replay 证明为复合，因此最终是 **73/73 raw coverage → 74/74 canonical decisions**）  
> 上游：`84fdeffd12f2ee307994d1eb6feb48173b6e0502`  
> 决策 map SHA-256：`6f533ba820d032e62ebdf33b7fedd0f30d405543088a880dc832a95fd67ad54f`  
> HEAD ledger SHA-256：`bc423f8c139f850d66c152571140ade2a011e80232f6ca0342d26e2771a05724`

## 结论

不安装任何 upstream 整包，也不把 upstream promotion 当成本地需求。74 个规范化候选原子全部有单一裁决：

- **ADAPT 10**：diagnosing source-side redaction 6；agent-doc 适用范围 1；environment/cache 被拆成两个独立 doctrine 2；codebase-design native dispatch/parity 1。
- **KEEP 18**：只保留 upstream lineage 或沿用已经存在的本地行为，**不是本地 activation**。
- **DEFER 19**：TDD pointer 1；logic-only HTML pilot 10；questionnaire one-off pilot 8。
- **REJECT 27**：whole-frontier rounds 5；wizard 12；phase tree 4；wait-what 注册/英语标准 3；distribution channel switch 3。
- **QUARANTINE 0**：HEAD-only 候选尚未在本地 live surface 激活；quarantine 留给已经可达的危险 live capability，不能拿它替代 REJECT。

最关键的独立修正有两个：

1. `MATT-fa7e31d36e28` 同时包含“environment 是 runtime SSOT”和“只 cache 高重获成本事实”，可独立失败，已规范化为 `MATT-c786c75d9ce5` 与 `MATT-8603aff9d903`；旧 ID 只留 1→2 coverage lineage。
2. 原 `head-ledger.json` 的 20 objects 不是 73 raw rows 的来源闭包：24 个候选 source paths 中漏 10 个。已补齐，并保留 6 个有明确 relevance 的 transitive refs，现为 30 objects、24/24 mandatory、0 missing。

## 决策语义

- `ADAPT`：只移植原子行为到 lucagstack 现有 SSOT/skill；不复制整包，不新增平行 authority。
- `KEEP`：对未采纳 HEAD delta 只表示保留谱系或维持现有覆盖；候选 diff 对 route/skill/pin/global target 必须为零。
- `DEFER`：只登记真实触发条件与一次性 pilot gate；触发前不建 route、skill、mode、gap 或 activation（questionnaire 只允许修正既有 gap 的 pilot gate）。
- `REJECT`：不进入执行面；replacement/deletion 仍写 ledger，防后续 blind refresh 复活。
- `QUARANTINE`：只用于本地已经可达且有即时危险的能力；本表没有这类 HEAD candidate。

## Family 汇总

| Family | 原子数 | 裁决 |
|---|---:|---|
| agent-doc-authoring | 4 | KEEP 4 |
| deletion-policy | 1 | KEEP 1 |
| diagnosing-redaction | 6 | ADAPT 6 |
| distribution | 3 | REJECT 3 |
| grilling-rounds | 7 | KEEP 2 / REJECT 5 |
| harness-neutral-dispatch | 3 | ADAPT 1 / KEEP 2 |
| logic-prototype | 10 | DEFER 10 |
| phase-boundary-tree | 7 | KEEP 3 / REJECT 4 |
| questionnaire | 8 | DEFER 8 |
| tdd-codebase-design | 1 | DEFER 1 |
| wait-what | 5 | KEEP 2 / REJECT 3 |
| wizard | 12 | REJECT 12 |
| writing-for-agents | 7 | ADAPT 3 / KEEP 4 |

## Work-package 映射

| Work package | HEAD 原子 | 边界 |
|---|---:|---|
| `WP-06-debug-redaction` | 6 | 改 shared systematic-debugging 的 source commands/HITL producer；双 harness canary，零 raw leak |
| `WP-07-codebase-parity` | 1 | 完整共享三文件、Claude symlink、Codex catalog、nested refs、真实 3+ native subagent receipts；不带 TDD pointer |
| `WP-10-authoring-and-defer-gates` | 11 | 三个 narrow authoring doctrines + 八个 questionnaire defer/pilot 原子；不注册 questionnaire |
| `WP-01-signed-decision-lineage-ledger` | 56 | KEEP/DEFER/REJECT、replacement 与 deletion 的不可变裁决；无 activation |

## Replacement / deletion lineage

| From | To | 裁决 |
|---|---|---|
| skills/in-progress/batch-grill-me/SKILL.md | skills/productivity/grilling/SKILL.md | REJECT_KILL_STANDS |
| skills/productivity/writing-great-skills/ | skills/productivity/writing-for-agents/ | ADAPT_TWO_DOCTRINES_ONLY |
| skills/in-progress/to-questionnaire/ | skills/productivity/to-questionnaire/ | DEFER_ONE_OFF_PILOT |
| skills/in-progress/wizard/ | skills/engineering/wizard/ | REJECT |
| terminal logic prototype | self-contained HTML logic demo | DEFER_ONE_OFF_PILOT |

删除的 6 条 skill lineage 全部为 `KEEP(upstream_lineage_only_no_local_delete)`；本地删除数必须为 0：

- `skills/deprecated/design-an-interface/SKILL.md`
- `skills/deprecated/qa/SKILL.md`
- `skills/deprecated/request-refactor-plan/SKILL.md`
- `skills/deprecated/ubiquitous-language/SKILL.md`
- `skills/personal/edit-article/SKILL.md`
- `skills/personal/obsidian-vault/SKILL.md`

## 逐原子裁决（74/74）

JSON 真值在 `head-decision-map.json`；下表逐项重复裁决、理由、双 harness 风险、work package 与机械门，便于人审。

| ID | Slug | Decision | WP | Reason | Claude/Codex parity risk | Mechanical gate |
|---|---|---|---|---|---|---|
| MATT-f785c73e1ec8 | tdd-interface-vocabulary-reference | DEFER | WP-01-signed-decision-lineage-ledger | 接口词汇指针有潜在价值，但本地尚无 seam 失败样本，且 codebase-design 当前只在 Claude 全局目录可达。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：A literal slash pointer can be reachable in Claude while dangling in Codex, or can accidentally start a second process instead of loading vocabulary. | 先改 tdd 会形成 Claude 可达、Codex dangling 的依赖边。 | 当前 tdd blob 与 route 保持不变；只有出现首个真实 seam ambiguity 且 WP-07 两端 parity 全绿后，才在一次性 reference-only pilot 中验证不会启动第二个 workflow。 |
| MATT-29d6533391d8 | redact-before-show | ADAPT | WP-06-debug-redaction | 本地 shared systematic-debugging 已实证会打印 IDENTITY 环境值和任意 HITL 错误文本，安全修复必须落在产生输出的命令或脚本源头。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：Masking only later prose still leaves raw tool output or transcripts exposed. | 只跑一端或只检查最终 prose 会产生假绿。 | 同一组 AWS/GitHub/Bearer/cookie/IDENTITY canary 在 Claude 与 Codex 的 raw stdout、stderr、transcript、artifact、final answer 中均为 0 字节命中。 |
| MATT-3fab1efa17e6 | credential-env-boundary | ADAPT | WP-06-debug-redaction | 本地 shared systematic-debugging 已实证会打印 IDENTITY 环境值和任意 HITL 错误文本，安全修复必须落在产生输出的命令或脚本源头。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：Command arguments or shell expansion can leak credential values even if later artifacts are redacted. | 只跑一端或只检查最终 prose 会产生假绿。 | 执行日志中的 argv/展示命令不含 credential 值，只有 SET/UNSET 或 <REDACTED>；凭据仅经环境注入。 |
| MATT-1cfdccc521e3 | artifact-signal-only | ADAPT | WP-06-debug-redaction | 本地 shared systematic-debugging 已实证会打印 IDENTITY 环境值和任意 HITL 错误文本，安全修复必须落在产生输出的命令或脚本源头。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：A minimally useful quote can still become a wholesale artifact disclosure if selection is not enforced. | 只跑一端或只检查最终 prose 会产生假绿。 | 含 auth header 的 fixture 只输出 allowlisted signal 行，原 artifact hash 可核但原文不进入 transcript。 |
| MATT-e900d56ff86c | insufficient-redaction-stop | ADAPT | WP-06-debug-redaction | 本地 shared systematic-debugging 已实证会打印 IDENTITY 环境值和任意 HITL 错误文本，安全修复必须落在产生输出的命令或脚本源头。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：A run may either leak more context or continue on an underdetermined diagnosis. | 只跑一端或只检查最终 prose 会产生假绿。 | 把诊断所需字段全部过滤后，两端返回 NEEDS_CONTEXT 并停止，不回读 raw artifact、不猜根因。 |
| MATT-087eaf541c55 | hitl-signin-not-capture | ADAPT | WP-06-debug-redaction | 本地 shared systematic-debugging 已实证会打印 IDENTITY 环境值和任意 HITL 错误文本，安全修复必须落在产生输出的命令或脚本源头。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：Using capture for login or credentials prints the value back to the terminal and agent. | 只跑一端或只检查最终 prose 会产生假绿。 | HITL 登录只出现 step，不存在 credential capture；输出字段由 allowlist 限定。 |
| MATT-1dea85381675 | hitl-unfiltered-free-text-gap | ADAPT | WP-06-debug-redaction | 本地 shared systematic-debugging 已实证会打印 IDENTITY 环境值和任意 HITL 错误文本，安全修复必须落在产生输出的命令或脚本源头。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：Secrets embedded in an error string bypass the prose-only redaction instruction. | 只跑一端或只检查最终 prose 会产生假绿。 | 把 secret sentinel 嵌入 ERROR_MSG，自由文本输出必须源头替换为 <REDACTED> 且诊断字段仍可用。 |
| MATT-c2613bbdf6f0 | writing-model-trigger-agent-docs | ADAPT | WP-10-authoring-and-defer-gates | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：Installing the whole skill adds a competing authoring authority and a new routing surface. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | 不安装 writing-for-agents、不新增 route；只把适用范围窄化写入现有 skill-authoring SSOT，并验证 Claude/Codex 指针都命中同一文件。 |
| MATT-4ef75cf060d9 | pointer-wording-controls-retrieval | KEEP | WP-01-signed-decision-lineage-ledger | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：A valid target behind weak wording is not loaded reliably. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | 该 atom 只进入 signed decision/lineage ledger；候选 diff 不新增 route、skill、pin 或 global target。 |
| MATT-cd5eca63e78c | real-context-boundary-for-step-hiding | KEEP | WP-01-signed-decision-lineage-ledger | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：An inline call leaves later steps visible and cannot deliver the claimed attention isolation. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | 该 atom 只进入 signed decision/lineage ledger；候选 diff 不新增 route、skill、pin 或 global target。 |
| MATT-c786c75d9ce5 | environment-is-runtime-ssot | ADAPT | WP-10-authoring-and-defer-gates | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：Static prose becomes the authority for a cheaply inspectable fact. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | skill-authoring 只新增一处 environment/cache canonical doctrine，CLAUDE.md 与 AGENTS.md 仅存指针；两端 agent-doc edit A/B 都实际加载同一 hash。 |
| MATT-8603aff9d903 | cache-only-costly-to-reacquire-facts | ADAPT | WP-10-authoring-and-defer-gates | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：A cheap fact is duplicated into prose. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | skill-authoring 只新增一处 environment/cache canonical doctrine，CLAUDE.md 与 AGENTS.md 仅存指针；两端 agent-doc edit A/B 都实际加载同一 hash。 |
| MATT-d32e20a647b2 | shared-reference-outside-user-only | KEEP | WP-01-signed-decision-lineage-ledger | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Duplicating the reference in each skill causes semantic drift; pretending one user-only skill can fire the other causes a dead edge. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | 该 atom 只进入 signed decision/lineage ledger；候选 diff 不新增 route、skill、pin 或 global target。 |
| MATT-17a0ed5e3b83 | router-hints-user-only | KEEP | WP-01-signed-decision-lineage-ledger | 本地 skill-authoring 已覆盖大部分写作理论；新增价值只在所有 agent-consumed docs 的适用范围和 environment/cache 原则，必须保持单一真值源。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Treating the router as an execution engine creates unreachable or falsely claimed dispatch. | 复制 doctrine 到两个 harness contract 会立即制造漂移源。 | 该 atom 只进入 signed decision/lineage ledger；候选 diff 不新增 route、skill、pin 或 global target。 |
| MATT-93c541c588be | logic-vs-visual-trigger | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Routing it into a polished visual prototype spends the wrong constraints and obscures the state-model question. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-1eb663064749 | visible-prototype-question | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：A correct-looking artifact can answer a different question without a checkable statement of intent. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-cbc9590cf27c | portable-pure-logic-module | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Embedding rules in UI handlers makes the learned behavior non-portable. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-1926a447c911 | dom-isolation-liftability | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：DOM coupling makes a visually working demo unusable as validated logic. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-68dd81ed3614 | self-contained-double-click-html | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：A runtime or install requirement removes the shareable-demo property. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-764808dd085a | domain-language-nondeveloper | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：The model may be technically testable yet opaque to the decision holder. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-0f66ea53c99a | full-state-rerender | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Partial or raw-JSON state hides illegal or surprising transitions. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-0f394f53d5d7 | free-play-actions | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Guided cases alone can miss emergent illegal sequences. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-8ec2838b5cad | deterministic-guided-scenarios | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：A walkthrough that does not reset or uses fake steps cannot be reproduced or compared. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-1ea3663314a9 | capture-logic-separate-shell | DEFER | WP-01-signed-decision-lineage-ledger | 单文件逻辑 demo 是独立 sliver，但与本地 framework 母版、docs/prototype 路径、traceability 和精修 UI 链存在边界冲突，且没有真实消费样本。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Shipping the shell or discarding the validated module confuses evidence with production implementation. | 两端若使用不同入口或不同产物路径，无法把一次性 pilot 当同一能力验证。 | 当前 route/catalog/gap/mode/pin 均无 logic-demo 新增；未来仅在真实 logic-only 任务产生一次性 artifact，并逐项验证本 atom 的状态/DOM/可分享/场景判准，且不改 framework/ 母版。 |
| MATT-dfe75cd8c7e9 | whole-frontier-rounds | REJECT | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Batching can overwhelm the user and bypass the local one-decision sharpening rhythm even if dependency ordering is correct. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 候选 diff 不把 whole-frontier/rounds 语义写入 grilling、triage 或 loop consumer；重启门仍要求两次真实不耐烦且 narrow-to-2 已失败。 |
| MATT-2d330e7516fe | question-format-recommendation | REJECT | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Formatting can pass while the number or cognitive breadth of simultaneous questions remains harmful. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 候选 diff 不把 whole-frontier/rounds 语义写入 grilling、triage 或 loop consumer；重启门仍要求两次真实不耐烦且 narrow-to-2 已失败。 |
| MATT-69c03fa3848c | dependency-respecting-frontier | REJECT | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A complete batch can still ask logically premature questions if dependency recalculation fails. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 候选 diff 不把 whole-frontier/rounds 语义写入 grilling、triage 或 loop consumer；重启门仍要求两次真实不耐烦且 narrow-to-2 已失败。 |
| MATT-3e8939104418 | fact-subagent-nonblocking | KEEP | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：A failed or unavailable subagent can silently turn a fact into a user question or block the whole round. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 只记录已存在的 fact/decision split 或 final human confirmation；不得借 KEEP 引入 rounds。 |
| MATT-3359aa28ff1a | frontier-empty-user-confirm | KEEP | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：An empty machine frontier can falsely stand in for human agreement. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 只记录已存在的 fact/decision split 或 final human confirmation；不得借 KEEP 引入 rounds。 |
| MATT-63cf1a43166c | triage-round-consumer | REJECT | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：The core skill can be rejected locally while a consumer silently preserves the same batch behavior. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 候选 diff 不把 whole-frontier/rounds 语义写入 grilling、triage 或 loop consumer；重启门仍要求两次真实不耐烦且 narrow-to-2 已失败。 |
| MATT-3601e39b1222 | loop-round-consumer | REJECT | WP-01-signed-decision-lineage-ledger | 上游 promotion 只改变供应侧成熟度；本地 whole-frontier 重启门仍未满足，结构化提问上限也不支持无界并发问题。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Deleting only the standalone batch skill leaves identical behavior active in downstream consumers. | 同一 rounds 文本在两端会产生不同批量与等待行为。 | 候选 diff 不把 whole-frontier/rounds 语义写入 grilling、triage 或 loop consumer；重启门仍要求两次真实不耐烦且 narrow-to-2 已失败。 |
| MATT-617e3a2a95a3 | questionnaire-claude-user-only | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Promotion alone can be mistaken for permission to add an automatic route. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-1c575c15b60e | questionnaire-codex-user-only | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Claude-only frontmatter parity can pass while Codex still invokes the skill automatically. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-e9258f150e5f | grill-send-not-subject | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Re-interviewing the user about facts they do not have cannot produce the missing evidence. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-1047ab54aad9 | recipient-gate | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Without a concrete recipient, tone and assumed context are fabricated. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-b38846688969 | needs-back-gate | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：A known recipient without a decision target yields a generic survey instead of a decision instrument. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-c64f15d32b8f | questionnaire-current-dir-output-coverage | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：An unpinned current directory can mis-scope the write even when question coverage passes. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-b3589d5156a5 | questionnaire-priority-and-themes | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：An async recipient may answer only an early portion, making weak ordering materially lossy. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-67f419958c76 | questionnaire-one-idea-answer-stub | DEFER | WP-10-authoring-and-defer-gates | 稳定毕业降低了上游成熟度疑虑，却没有替代本地具名收件人和明确回收目标的真实触发证据；任何试点都必须写入 pinned project path。因此当前只冻结触发器和试点判准，不新增 route、skill、mode、pin 或 activation。 该行为若失守的独立退化是：Compound questions or detached answer areas reduce completion and attribution quality. | 把 metadata parity 冒充本地可执行 parity 会造假；试点只能走共同自然语言触发。 | Claude/Codex 都不新增 questionnaire 注册；gap 只允许在具名 recipient 与明确 needs-back 两个人类门通过后做一次性 pilot，输出写绝对 pinned project path，不能写 current directory。 |
| MATT-ba5ba30d55fc | wizard-model-trigger-human-only | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：An automatic credential or cutover trigger can cross human, account, repository, and irreversible-action gates without explicit authorization. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-995d533d88fa | wizard-codex-model-metadata | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Claude and Codex can diverge in automatic activation semantics even when both display the skill. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-addf618cdf4b | wizard-scope-stage-destinations | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A complete stage list can still target the wrong account or repository if identity is not pinned separately. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-4df741edbd67 | wizard-no-invented-journey | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A correct stage schema can still send the human through a stale or nonexistent external interface. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-e1881ecf0f86 | wizard-fixed-template-stages | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Treating the library as immutable can preserve unsafe helpers and hides version or local-policy changes. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-e072e08dc8b4 | wizard-confirm-irreversible | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Reversible external writes and credential changes still occur without per-write preview, confirmation, or compensation. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-9e17e436b51c | wizard-static-only-verification | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：External-write, quoting, rollback, account, and cancellation faults remain untested. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-759e33cc418f | wizard-cross-platform-url-open | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Opening the right URL does not pin account, tenant, or repository identity. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-af30e0ed4176 | wizard-secret-read-reuse | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Secret existence and content cross from disk into process memory without identity, permission, or redaction guarantees. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-d630109f0348 | wizard-env-upsert | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：There is no backup, rollback, gitignore preflight, escaping contract, or per-write approval; embedded newlines can corrupt the file. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-a9b1e31a35ad | wizard-github-external-writes | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Authentication can point at the wrong account or repository, and a partial set of writes has no compensating rollback. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-9cd8048bc76e | wizard-partial-success-summary | REJECT | WP-01-signed-decision-lineage-ledger | 该能力会打开 GUI、读取或写入凭据、修改 .env 和 GitHub 外部状态，却缺少目标身份绑定、逐写审批、事务日志和补偿回滚。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A friendly summary can normalize a partially committed state without rollback or an explicit failed transaction status. | 两端都缺事务性外部写合同，而触发语义还不一致。 | wizard、template.sh 与 metadata 在 route、catalog、pin、global skill targets 中均不存在，且没有 plugin/skills.sh 间接安装；任何命中立即 FAIL。 |
| MATT-7e6c82da4918 | phase-boundary-only | KEEP | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Applying the tree mid-phase discards the live reasoning thread. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 只在 ledger 记录已有 phase-boundary/AFK delegation/lossiness 原则；本地 checkpoint、handoff 与 native compaction 合同 hash 不变。 |
| MATT-768dcd6a9f31 | phase-continue-primary-source-smart-zone | REJECT | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：The hard-coded window estimate and manual judgement are model- and harness-specific and can override actual compaction behavior. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 共享合同和候选 diff 中不新增 /clear、/compact、150k smart-zone 或 portability-only handoff 分支。 |
| MATT-32a587e2ae16 | phase-clear-irrelevant-context | REJECT | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A Claude slash assumption is not portable to Codex, and a mistaken relevance judgement irreversibly loses the why. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 共享合同和候选 diff 中不新增 /clear、/compact、150k smart-zone 或 portability-only handoff 分支。 |
| MATT-14788bc00c7c | phase-handoff-portability-only | REJECT | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：This narrow definition conflicts with lucagstack's durable mandatory handoff artifacts and shared state semantics. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 共享合同和候选 diff 中不新增 /clear、/compact、150k smart-zone 或 portability-only handoff 分支。 |
| MATT-1ef0003875bf | phase-afk-subagent | KEEP | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Prompt-only scope can be falsely treated as execution isolation or completion proof. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 只在 ledger 记录已有 phase-boundary/AFK delegation/lossiness 原则；本地 checkpoint、handoff 与 native compaction 合同 hash 不变。 |
| MATT-cacaba6fee83 | phase-compact-default | REJECT | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Codex does not expose this slash contract, while both harnesses already perform automatic compaction differently. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 共享合同和候选 diff 中不新增 /clear、/compact、150k smart-zone 或 portability-only handoff 分支。 |
| MATT-14aa6fda7115 | phase-primary-secondary-lossiness | KEEP | WP-01-signed-decision-lineage-ledger | 只有 phase-boundary 与 primary-source lossiness 原则可复用；/clear、/compact、150k 和 portability-only handoff 是 Claude/模型特定语义。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Treating every handoff or compaction as equally lossy ignores structured durable artifacts and native session receipts. | 共享树会让同一分支在 Claude 可执行、在 Codex dangling。 | 只在 ledger 记录已有 phase-boundary/AFK delegation/lossiness 原则；本地 checkpoint、handoff 与 native compaction 合同 hash 不变。 |
| MATT-88bd946f2af3 | wait-what-claude-user-only | REJECT | WP-01-signed-decision-lineage-ledger | 自然语言纠正已经是双 harness 原生触发；独立 skill 增加 catalog 与认知负担，ASD-STE100 也不适合作为中文清晰度规范。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A dedicated corrective command increases cognitive and routing load for a trigger already expressed in natural language. | 真正共同不变量是按用户语言重述，不是同名注册。 | route/catalog/global skill 中无 wait-what，且共享中文行为规范不出现 ASD-STE100 强制。 |
| MATT-ef3806a9c4af | wait-what-codex-user-only | REJECT | WP-01-signed-decision-lineage-ledger | 自然语言纠正已经是双 harness 原生触发；独立 skill 增加 catalog 与认知负担，ASD-STE100 也不适合作为中文清晰度规范。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Adding the registration still spends catalog and human-index cost despite correct invocation parity. | 真正共同不变量是按用户语言重述，不是同名注册。 | route/catalog/global skill 中无 wait-what，且共享中文行为规范不出现 ASD-STE100 强制。 |
| MATT-826719117fa0 | wait-what-repitch-context | KEEP | WP-01-signed-decision-lineage-ledger | 自然语言纠正已经是双 harness 原生触发；独立 skill 增加 catalog 与认知负担，ASD-STE100 也不适合作为中文清晰度规范。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：A rephrasing can remain opaque if it merely adds more explanation without changing the mental model. | 真正共同不变量是按用户语言重述，不是同名注册。 | 普通自然语言纠正能在两端触发重述，并沿 active CONTEXT 或用户当前用词降级；不创建技能入口。 |
| MATT-36a37a56c05b | wait-what-asd-ste100 | REJECT | WP-01-signed-decision-lineage-ledger | 自然语言纠正已经是双 harness 原生触发；独立 skill 增加 catalog 与认知负担，ASD-STE100 也不适合作为中文清晰度规范。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：An English technical-writing standard does not guarantee clarity in Chinese and can distort natural product language. | 真正共同不变量是按用户语言重述，不是同名注册。 | route/catalog/global skill 中无 wait-what，且共享中文行为规范不出现 ASD-STE100 强制。 |
| MATT-3032fe812104 | wait-what-context-vocabulary | KEEP | WP-01-signed-decision-lineage-ledger | 自然语言纠正已经是双 harness 原生触发；独立 skill 增加 catalog 与认知负担，ASD-STE100 也不适合作为中文清晰度规范。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：A missing or stale context file can replace the user's current words with outdated jargon. | 真正共同不变量是按用户语言重述，不是同名注册。 | 普通自然语言纠正能在两端触发重述，并沿 active CONTEXT 或用户当前用词降级；不创建技能入口。 |
| MATT-df4775134f2f | code-review-harness-neutral | KEEP | WP-01-signed-decision-lineage-ledger | 去除厂商工具名只是文案可移植性，不等于原生 agent 可发现、可派发、隔离和 receipt 已闭合。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：A heading can remain portable while the local harness lacks the required role, isolation, or receipt mechanics. | 删除 vendor 名称只能通过文案 gate，不能通过执行 parity gate。 | 只记上游措辞 lineage；不存在因措辞变化而新增的本地 role、route 或 capability PASS。 |
| MATT-2e91f1b00c08 | codebase-design-harness-neutral | ADAPT | WP-07-codebase-parity | 去除厂商工具名只是文案可移植性，不等于原生 agent 可发现、可派发、隔离和 receipt 已闭合。因此只吸收这个独立行为并接入本地 SSOT/安全边界，禁止整包安装。 该行为若失守的独立退化是：The source may be portable while Codex catalog reach, nested references, or native parallel dispatch is absent. | 删除 vendor 名称只能通过文案 gate，不能通过执行 parity gate。 | 共享目录的 SKILL.md、DESIGN-IT-TWICE.md、DEEPENING.md 三文件闭包存在；Claude 为指向共享真身的软链；Codex catalog 可发现；两端真实派发 3+ native subagents 且 receipt 的 parent/child/role/hash 可核。 |
| MATT-5ae242a1e7a3 | architecture-scan-harness-neutral | KEEP | WP-01-signed-decision-lineage-ledger | 去除厂商工具名只是文案可移植性，不等于原生 agent 可发现、可派发、隔离和 receipt 已闭合。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Removing a vendor name can still leave an unavailable capability or unsafe write-enabled explorer. | 删除 vendor 名称只能通过文案 gate，不能通过执行 parity gate。 | 只记上游措辞 lineage；不存在因措辞变化而新增的本地 role、route 或 capability PASS。 |
| MATT-ec1e2352340c | claude-official-plugin-auto-update | REJECT | WP-01-signed-decision-lineage-ledger | 上游按 Claude plugin 与 Codex skills.sh 分渠道交付；本地采用 pinned source、共享真身、harness adapter 与 FUSION 人类门，不能切到自动更新或直拷通道。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：Automatic upstream updates bypass lucagstack's pinned source review, adaptation, and gated activation. | 采用上游分渠道会拆开本地一份真身、两端 adapter 的架构。 | 候选 diff 不执行或推荐 Claude official plugin、skills.sh 直拷或二者 exclusivity；installed-pins、watcher、shared target 与 H4/FUSION 控制链保持唯一通道。 |
| MATT-875493a46768 | codex-skills-sh-copy | REJECT | WP-01-signed-decision-lineage-ledger | 上游按 Claude plugin 与 Codex skills.sh 分渠道交付；本地采用 pinned source、共享真身、harness adapter 与 FUSION 人类门，不能切到自动更新或直拷通道。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：A copied file can bypass local lineage pins, local modifications, and dual-harness acceptance gates. | 采用上游分渠道会拆开本地一份真身、两端 adapter 的架构。 | 候选 diff 不执行或推荐 Claude official plugin、skills.sh 直拷或二者 exclusivity；installed-pins、watcher、shared target 与 H4/FUSION 控制链保持唯一通道。 |
| MATT-83b5baf3a2a6 | exclusive-install-routes | REJECT | WP-01-signed-decision-lineage-ledger | 上游按 Claude plugin 与 Codex skills.sh 分渠道交付；本地采用 pinned source、共享真身、harness adapter 与 FUSION 人类门，不能切到自动更新或直拷通道。因此不进入本地执行面，只保留可审计的拒绝与 replacement 谱系。 该行为若失守的独立退化是：luca_gstack deliberately has shared truth plus harness adapters, so copying this rule literally would misclassify its architecture. | 采用上游分渠道会拆开本地一份真身、两端 adapter 的架构。 | 候选 diff 不执行或推荐 Claude official plugin、skills.sh 直拷或二者 exclusivity；installed-pins、watcher、shared target 与 H4/FUSION 控制链保持唯一通道。 |
| MATT-7fa068e58440 | docs-renderer-install-ssot | KEEP | WP-01-signed-decision-lineage-ledger | 这些规则服务上游 aihero 文档发布面；可复用原则要么已由本地质量门覆盖，要么被 environment/cache doctrine 吸收，不形成新运行时能力。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Hand-written copies drift from the executable or rendered source of truth. | 若只记谱系则无 parity 风险；若当 runtime doctrine 安装会引入第三份 SSOT。 | 只写 lineage；不创建本地 docs-page skill/route，不复制 aihero renderer 规则；被 environment/cache 吸收的含义只在 canonical doctrine 出现一次。 |
| MATT-864d55193e5e | observed-questions-evidence | KEEP | WP-01-signed-decision-lineage-ledger | 这些规则服务上游 aihero 文档发布面；可复用原则要么已由本地质量门覆盖，要么被 environment/cache doctrine 吸收，不形成新运行时能力。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Invented padding can satisfy structure while answering questions no user has. | 若只记谱系则无 parity 风险；若当 runtime doctrine 安装会引入第三份 SSOT。 | 只写 lineage；不创建本地 docs-page skill/route，不复制 aihero renderer 规则；被 environment/cache 吸收的含义只在 canonical doctrine 出现一次。 |
| MATT-a624582e0135 | external-working-signals | KEEP | WP-01-signed-decision-lineage-ledger | 这些规则服务上游 aihero 文档发布面；可复用原则要么已由本地质量门覆盖，要么被 environment/cache doctrine 吸收，不形成新运行时能力。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：An internal compliance check can masquerade as a user-observable outcome. | 若只记谱系则无 parity 风险；若当 runtime doctrine 安装会引入第三份 SSOT。 | 只写 lineage；不创建本地 docs-page skill/route，不复制 aihero renderer 规则；被 environment/cache 吸收的含义只在 canonical doctrine 出现一次。 |
| MATT-9ebf3141af1c | branches-table-list | KEEP | WP-01-signed-decision-lineage-ledger | 这些规则服务上游 aihero 文档发布面；可复用原则要么已由本地质量门覆盖，要么被 environment/cache doctrine 吸收，不形成新运行时能力。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：Correct branch prose can remain hard to scan and easy to miss. | 若只记谱系则无 parity 风险；若当 runtime doctrine 安装会引入第三份 SSOT。 | 只写 lineage；不创建本地 docs-page skill/route，不复制 aihero renderer 规则；被 environment/cache 吸收的含义只在 canonical doctrine 出现一次。 |
| MATT-28d950692bef | retired-skills-deleted | KEEP | WP-01-signed-decision-lineage-ledger | 上游删除和 replacement 只构成谱系事实；本地没有同名 installed/adopted counterpart，不能据此删除本地资产。因此 KEEP 只表示保留上游谱系或沿用已经存在的本地行为，不授权本地新增激活。 该行为若失守的独立退化是：A blind refresh could either resurrect removed upstream skills or delete unrelated local capabilities with similar names. | 唯一风险是盲同步删除误伤名称相近但来源不同的本地能力。 | replacement/deletion ledger 完整，但候选 diff 删除的本地 skill 文件数为 0。 |

## 机械完整性

已实际得到：

`raw=73 / raw coverage=73 / canonical=74 / IDs unique=74 / decisions=74 / unclassified=0 / whole-package-install=false`。

复放命令：

`jq -e '(.entries|length)==74 and ([.entries[].id]|unique|length)==74 and (.lineage.raw_to_canonical_coverage|length)==73 and (.summary.decisions.ADAPT==10) and (.summary.decisions.KEEP==18) and (.summary.decisions.DEFER==19) and (.summary.decisions.REJECT==27) and (.summary.decisions.QUARANTINE==0)' head-decision-map.json`

`head-candidate-census.json` 的 24 个 blob OID/SHA/bytes/EOF、73 raw ranges，以及 `reconciled-census.json` 的 74 个 content IDs 均已对只读 upstream clone 重算，错误数 0。

<!-- FILE_END: head-decision-recommendations.md -->
