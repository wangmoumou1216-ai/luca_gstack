You are the cold independent redteam reviewer requested explicitly by the user: 完成后由红队 review，发现问题解决，无问题提交发布. Review the memory evolution delivery and release readiness. This is a framework NO_PIN task, not a downstream project. Do not receive or ask for implementation conversation history.

Use .claude/skills/office/redteam/SKILL.md and applicable root/office contracts, routing-chain-check R4, model-routing reasoning-heavy criteria. Explicit target: /private/tmp/memory-release-redteam/manifest.json (72 exact files), frozen files at files/, tracked.patch, and unpublished-ancestors.patch (two pre-existing unpushed routing commits that publication would carry). Requirements and assertions are the frozen memory-evolution-plan.md and results/research artifacts. These outputs are claims to challenge, not proof of correctness. Do not broaden into unrelated working tree WIP.

Constraints: no edits to repository/live memory/index, no Git commit/push/project switch, no live pending disposition, no migrations --apply, no reading actual downstream project content, no child agents. You may create and mutate ISOLATED TEMP fixtures under /private/tmp for meaningful behavioral/fault/mutation probes; workspace-write sandbox is granted for this purpose, not implementation edits. Read the frozen candidate, compare exact hashes if executing live code. Treat framework/ templates read-only. Finish with a report only; the primary agent will fix confirmed issues.

Attack decision assumptions, actual memory lifecycle, scope correctness, evidence preservation/retry, governed promotion, research claims vs observed behavior, code/data checkout split, unattended scheduling, real vs mocked harness proof, and release consistency. Standards/Spec earlier PASS must not make you assume correctness. In particular determine whether the described limitations are honestly bounded or conceal an unresolved requirement. Native Claude API403 is known UNKNOWN; do not label it PASS or invent credentials. Propose no new architecture without showing a concrete current defect. Existing limitations should be identified with scope/impact, not automatically widened into a rewrite.

Run narrow isolated runtime partitions and at least one mutation where coverage is claimed; pure static review is insufficient. Do not run external online model probes or full native nested sessions. Test results already in artifacts may substantiate only the exact tested bytes. Do not silently erase a failed probe; distinguish environment failure from a test finding. You may inspect relevant current active rules and recent observations per skill, not full history.

Output Chinese redteam report: PASS/FAIL for release readiness, each question one sentence + if true impact, severity and oracle/taste classification, exact file/line/behavior evidence, proven vs unknown, concrete unresolved blockers. Do NOT implement solutions. State actual tests/probes run and limitations. Be concise but evidence-based. Return report as final output; primary will save it to framework-audit/2026-09-08-memory-evolution-redteam.md. User authorized this review; no further approval is needed.
