# Design Brief — {功能名称}

生成时间：YYYY-MM-DD HH:MM
场景：{新功能设计（A）/ 已有功能优化（B）/ 评审改版（C）/ Agent 化改造（D）}
输入来源：{ux-brainstorm / ux-research / ux-audit / PRD / 用户粘贴方案 / N/A（场景C）}
**母版：{列表页 / 详情页（两列） / 详情页（三列） / 表单页 / 首页 /
AI速记入口页 / 录音工作页 / 局部改动/独立组件}**
来源 PRD：{文件路径}
来源 constraints：{路径，场景 B/D 专有}
来源 ux-audit：{路径，场景 C 专有}

---

## 设计坐标系

**必须解决的问题：**
- {问题ID}：{问题简述}

**可借鉴的范式：**
- {范式名称}：{对本次设计的含义}

**不能碰的范围：**
- {Not-Do 条目}

**AI Native 方向：**
- 结论：AI {介入 / 不介入}
- [若介入] 范式：{对话式替代 / 嵌入式预测 / 代理式执行 / 决策增强}
- [若介入] 介入节点：{具体节点}
- [若不介入] 理由（4 选 1）：{超出语料 / 竞品证据 / 技术前提 / 场景不适合} — {一句话}

---

## 原生AI深度思考小结

**Layer A · 产品层：**
决策压缩：{N} 次 → {N'} 次主动判断（压缩 {N-N'}，必须 ≥ 2）
采纳的 AI 介入：{描述 / 暂不可行：原因}

**Layer B · 交互层：**
传统路径：{N} 步 → AI 原生路径：{M} 步
可行性：{高/中/低}
关键假设：{前提条件}
Fallback：{失败路径}

**Layer C · 信任层（AI 功能必填）：**
不确定性表达方式：{引用依据 / 置信度 / hedging / 多候选 / 预览-执行 / 无 AI}
首次用户建立信任的路径：{说明 / N/A}
AI 错了用户多久能发现：{说明 / N/A}

**Layer D · 代理层（场景 D / agent 功能强制）：**
过程可见：{是 + 形式 / 否 / N/A}
可暂停：{是 + 暂停后状态 / 否 / N/A}
可接管：{是 + 接管流程 / 否 / N/A}
可撤销：{是 + undo 粒度 / 否 / N/A}
授权边界：{说明 / N/A}

**对设计决策的影响：**
{描述采纳了什么，预留了什么接口}

---

## 假设挑战结论

核心假设：
- 假设 1：{描述}
- 假设 2（可选）：{描述}

最脆弱的假设：{假设 N} → 建议在 {节点} 验证

【场景 D 专有】信任假设挑战：
- 假设："用户会信任 agent 自动执行 {动作}"
- 若用户不信任的 fallback：{方案}

结论：{方案继续 / 需要调整：{具体} / 建议 fallback：{具体}}

---

## 体验验证结论

P0 故事覆盖：{N}/{N} 条通过
核心任务路径：{N} 步
设计边界检查：{没有触碰"不做什么" / 有：{描述}}

### 状态覆盖表（12 状态全必须声明）

| 状态 | 方案处理方式 | 是否需要单独设计 |
|------|-----------|---------------|
| 默认态 | {描述} | {是/否} |
| 空态 | {描述 / N/A：原因} | {是/否} |
| 加载态 | {描述 / N/A：原因} | {是/否} |
| 错误态 | {描述 / N/A：原因} | {是/否} |
| 成功态 | {描述 / N/A：原因} | {是/否} |
| 思考中态 | {描述 / N/A：原因} | {是/否} |
| 低置信态 | {描述 / N/A：原因} | {是/否} |
| 拒答态 | {描述 / N/A：原因} | {是/否} |
| 部分完成态 | {描述 / N/A：原因} | {是/否} |
| 待 Steer 态 | {描述 / N/A：原因} | {是/否} |
| 幻觉兜底态 | {描述 / N/A：原因} | {是/否} |
| Agent 执行中态 | {描述 / N/A：原因} | {是/否} |

（场景 A/B 无 AI 功能时，AI 专有 7 项统一写 "N/A — 本功能不涉及 AI"。场景 D 不允许 N/A。）

---

## 品味检查四锚点

（节名保持原版，内部扩展为 8 锚点三维度）

### 效率维度（5 锚点）

- **Ryo Lu**：每个 UI 元素能回答「帮用户完成什么任务」？→ [是/否，如否列出]
- **Linear**：核心任务路径步骤数？→ [N 步]；能再少？→ [是/否]
- **Attio**：信息层次 L1/L2/L3 可辨认？→ [是/否]
- **Notion**：首屏主要操作入口数量？→ [N 个]；有无重复？→ [有/无]
- **Raycast**：AI 入口在任务上下文内？→ [是/否/无 AI]；AI 介入后路径变长？→ [是/否/无 AI]

### 信任维度（新增 2 锚点）

- **Perplexity**：AI 输出可追溯（来源/推理/置信度）？→ [是/否/无 AI]
  不通过点：{列出}
- **Granola**：AI 诚实表达不确定性？→ [是/否/无 AI]
  过度自信点：{列出}

### 代理维度（新增 1 锚点，场景 D / agent 功能强制）

- **Cursor**：AI 动作可见/可暂停/可接管/可撤销？→ [全部是/部分是/无 agent]
  不通过点：{列出}

### 整体评估

| 锚点 | 结果 | 严重性 |
|-----|------|------|
| Ryo Lu | {通过/不通过} | 警告 |
| Linear | {通过/不通过} | 阻断 |
| Attio | {通过/不通过} | 警告 |
| Notion | {通过/不通过} | 警告 |
| Raycast | {通过/不通过/N/A} | 阻断 |
| Perplexity | {通过/不通过/N/A} | 阻断 |
| Granola | {通过/不通过/N/A} | 阻断 |
| Cursor | {通过/不通过/N/A} | 阻断（场景 D）|

结论：{全部通过 / 阻断项: {列出} / 警告项: {列出}}

---

## 原型决策清单

（每条决策必须完整 8 字段，缺任一项该决策不输出）

### D-001：{组件/模式名称}

- **决策内容：** {做什么}
- **设计理由：** {必须引用 PRD P0 / ux-audit / native-design / inspired-design / framework}
- **排除的备选方案：** {≥ 1 条}
  - 备选 1：{描述} → 不选原因：{原因}
- **接受的 tradeoff：** {不为"无"}
- **AI Native 判定引用：** {引用设计坐标系的全局结论 + 本决策的体现}
- **状态覆盖：** {引用 Phase 3 表格中与本决策相关的状态}
- **PRD 约束引用：** {prd-constraints.md 条目 / 场景 A 可写 N/A}

### D-002：...

（按 P0 任务顺序逐条输出）

---

## shadcn 组件映射表

| 页面/状态名称 | 区域 | 组件来源 | shadcn 组件名 | variant | 对应决策 ID | 备注 |
|-------------|------|---------|--------------|---------|-----------|------|
| {页面名} | {区域} | shadcn | Button | default | D-001 | 主操作，#FF8000 |
| {页面名} | {区域} | 自绘 | — | — | D-003 | {Tailwind+颜色+间距} |

**AI 功能区域特殊标注：**
- 思考中态（ghost text / skeleton / streaming）：{描述}
- 低置信态（视觉降级）：{描述}
- 拒答态（文案区）：{描述}

---

## REMOVED 记录（场景 B / D）

- {决策描述}：REMOVED: OUT OF SCOPE — {原因}

（无则写「无」）

---

## 交接块（下游 skill 必读）

**本步决定了什么：**
- 信息架构、主要组件选择、关键交互路径
- AI Native 范式选择及介入节点
- 12 状态的覆盖策略（包含 AI 专有状态）
- Agent 的授权边界（仅场景 D）

**下游 /html-prototype 需要知道：**
- 设计范围（来自 PRD）
- 框架类型（母版名称）
- 状态覆盖策略（12 状态中哪些需要单独设计）
- 映射表路径
- **AI 专有状态的 UI 形式** — 本表 12 状态中 AI 专有 7
  项（思考中/低置信/拒答/部分完成/待 Steer/幻觉兜底/Agent 执行中），
  默认实现不包含这 7 项，含 AI 功能时**必须**额外生成所有非 N/A 项

**下游 /html-prototype 不应该做：**
- 不应重新设计已锁定的信息架构
- 不应绕过映射表选用其他组件
- **不应省略任何非 N/A 状态的实现** — 用 `<!-- STATE: xxx -->` 注释标注每个状态页
- 不应把 AI 输出做成"无来源无置信度"的纯文本
- 不应用浮球 / 新标签页作为 AI 入口

**对 /html-prototype 的命令式指示：**
1. 读取本文档的"体验验证结论"节 12 状态覆盖表
2. 对每个"是否需要单独设计 = 是"的状态，**必须**生成对应状态页
3. 发现状态表某项写 N/A 但本质应有时，不得静默补充，必须 AskUserQuestion 确认

**下游 /figma-layer 需要知道：**
- 同上
- 组件内 shadcn 优先，跳出组件的部分以 HTML 为准

<!-- FILE_END: design-brief/SCHEMA.md -->
