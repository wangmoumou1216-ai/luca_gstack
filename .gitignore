# OS
.DS_Store
Thumbs.db

# Environment — block ALL .env variants except .example templates
.env
.env.local
.env.*.local
.env.*
!.env.example
!.env.*.example

# Dependencies
node_modules/

# Build outputs
dist/
dist-cjs/
build/
out/
output/

# TypeScript
*.tsbuildinfo

# Python
*.py[cod]
*$py.class
*.egg
*.egg-info/
*.so
__pycache__/
.Python
.cache/
.pnpm-store/

# Routing eval 运行产出（fixtures.jsonl 跟踪；judge 工作单是每次运行的临时产物，不跟踪）
memory/evals/routing/judge-queue-*.jsonl

# SQLite / Database
*.db
*.db-journal
*.db-wal
*.sqlite
*.sqlite-journal
*.sqlite-wal

# Logs
*.log
*.pid
*.pid.lock
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
mcp-server.log
init-output.log

# Test artifacts
coverage/
.nyc_output/
test-output/
test-reports/
tests/logs/
tests/results/

# Temp files
*.tmp
*.temp
*.swp
*.swo
*~

# IDE
.vscode/
.idea/

# Misc
.turbo/
.deno/
.research/
.mcp.json
api-project/

# Claude Code local settings (machine-specific, not shared)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/.session-turn-count
.claude/.session-edit-count
.claude/.session-tool-count
# per-session 后缀变体（G2 并发隔离）与切换互斥锁 + G6 会话粘性 pin/nag
.claude/.session-turn-count-*
.claude/.session-edit-count-*
.claude/.session-tool-count-*
.claude/.session-project-*
.claude/.session-consumed-turns-*
# E2 义务状态文件：含 `exact_task_text` = **完整原始 prompt 字节**（上限 262,144 B）。
# 必须先于义务落盘代码进入本文件，否则每个产生义务的 session 都会留下一个含用户原话的
# 未跟踪文件，一次 `git add -A` 就把它提交进仓库。文件名刻意不以 `.session-project-` 开头
# （`scripts/check-project-links.mjs:59` 会把那个前缀当项目 pin 误解析）。
.claude/.session-obligation-*
.claude/.session-projnag-*
.claude/.session-inherited-*
.claude/.project-switch.lock/
.claude/project-state-quarantine/
.claude/observability/pending-extraction*.md
.claude/.episode-written-*
.claude/.digest-shown-*
.claude/current-topic*.txt
.claude/workflow-state*.yaml
# code-hygiene skill cleanup reports — transient build artifacts
.claude/cleanup-reports/

# Memory system — ignore raw runtime data, keep scripts and stable knowledge
memory/episodic/sessions/*.md
!memory/episodic/sessions/README.md
memory/episodic/.index.lock
memory/semantic/candidates.jsonl
# reviews.jsonl / retrieval-log.jsonl 自 2026-07-15 入库（记忆层评审）：前者是晋升/拒绝
# 审计轨迹（被审计对象 promoted-facts 有备份、审计轨迹自己是单机孤本说不通），
# 后者喂 ADR-0006 架构裁决（决策数据集不可审计=裁决不可信）。candidates 仍属易变队列不入库。
# Daily governance digests — regenerated from memory state each day
memory/digests/
# Monthly framework-evolution HTML reports — generated visual artifacts (evolution/digests/*.md is the tracked truth)
.claude/skill-os/evolution/report-*.html
# Observability — runtime data, regenerated each session
# (pending-extraction*.md 含 per-session 后缀变体，见上方 G2 并发隔离节)
.claude/observability/run-log.jsonl

# luca_gstack specific — docs/ is a symlink to external project dir, never tracked
docs

# external-skill: extract-design-system runtime output (竞品 tokens — keep out of env repo)
.extract-design-system/
design-system/
.claude/.allow-framework-write
# standalone/cloud opt-in marker（勿提交；判别器据此把 auth-absent 从 anomaly 降 note，R8/R3A-1）
.claude/.memory-standalone

# hook 写的每日提示标记（.kit-relay-shown-YYYY-MM-DD）：纯本机状态，不是产物。
# 不入库——否则每天新增一个未跟踪文件，把 git status 的信噪比长期压低。
.claude/.kit-relay-shown-*

# playwright CLI 的调试转储（console-*.log / page-*.yml）：工具运行时产物，非仓库资产。
# 同上信噪比理由（2026-09-02：一批 8/26 的转储长期挂在 untracked 面上）。
.playwright-cli/

# record_eval.py 的 flock 锁文件：空文件、不持有锁（锁随 fd 关闭释放），纯本机运行时痕迹。
memory/evals/.eval-log.lock
