# 记忆单一权威 store（2026-07-09 final-plan/decide-ledger F5-02/03，收窄范围执行）
#
# 母版 = 全部记忆层单一权威。muse app 注入 MEMORY_ROOT=母版，fork session 记忆读写落母版。
# 下列 append-only 会话流水 fork↔母版 天然发散，sync-upstream（git merge upstream/main）时
# 保 fork 版、不合并母版版，根除每次 merge 冲突。需 `git config merge.ours.driver true`
# 已由 sync-upstream.sh 幂等注册。
#
# ⚠️ 收窄范围（勿扩到整个 memory/semantic/**）：策展文件——promoted-facts.yaml（框架稳定事实）、
# static-fallback-allowlist.txt（白名单）、各 README.md——**不在此列，仍正常继承母版更新**，
# 否则 fork 会静默漂移、继承不到母版新晋升的框架知识。
memory/episodic/index.jsonl merge=ours
memory/episodic/archive/** merge=ours
memory/semantic/archive/** merge=ours
